import { ImageFormatDirective } from './image-format.directive';

describe('ImageFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFormatDirective();
    expect(directive).toBeTruthy();
  });
});
